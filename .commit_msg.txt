🚀 DEPLOY: TouraLuxe Sovereign Optimization, Auth Resilience & Real-Time Sync

✨ FEATURES:
- **JIT Booking Association**: Automatically link guest bookings to user profiles via `customerEmail` queries in the API when `userId` is absent.
- **Real-Time Booking Sync**: Established live Supabase subscriptions in the traveler portal, paired with a 10s polling interval and visibility API listeners.
- **Centralized User Control**: Unified logout functionality into the master Modal Shell for enhanced UI consistency.

⚡ PERFORMANCE & OPTIMIZATION:
- **Off-Thread ImageBitmap Decoding**: Upgraded the `HeroSequence` canvas engine to parse frames via background `createImageBitmap` worker threads, drastically reducing main-thread blocking.
- **Instant VRAM Cleanup**: Replaced heavy HTMLImageElement DOM garbage-collection overhead with deterministic `ImageBitmap.close()` releases.
- **Hardware-Accelerated Layouts**: Implemented `contain-paint` on the Hero container, applied `transform-gpu` to canvas sequences, and forced off-thread decoding (`decoding="async"`) on critical detail images.
- **iOS WebGL Smoothness**: Stopped dynamic animation of heavy CSS backdrop-blur filters, and bypassed iOS GPU reallocations by keeping the WebGL canvas visibility container active while scrolling.

🔧 RESILIENCE & FIXES:
- **Concurrency Circuit Breaker**: Wrapped profile fetching and auth session loading with `Promise.race` and an absolute 5s fallback timeout to prevent infinite page loading.
- **Supabase Lock Hardening**: Set `multiTab: false` on client initialization to eliminate "Lock stolen" AbortErrors during hot-reloads or multi-tab usage.
- **React Strict Mode Stability**: Disabled strict mode during dev iterations to prevent double-execution side-effects on state hooks and WebGL context allocations.
- **Cloud-Break Flight Correction**: Moved the initial plane coordinates to deep space off-screen (`x: 1500`) to guarantee no initial overlap with the landing layout.
